name: skin Analytics Daily

on:
  schedule:
    - cron: '0 6 * * *'  # 毎日 06:00 JST - Data Collection
    - cron: '0 7 * * *'  # 毎日 07:00 JST - Analysis & Optimization
  workflow_dispatch:

jobs:
  analytics_pipeline:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: pip install requests python-dotenv

      # Step 4: Collect data from Threads API
      - name: Collect data from Threads API
        env:
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
        run: python3 skin/skin_data_collector.py

      # Step 5: Analyze patterns
      - name: Analyze patterns
        run: python3 skin/skin_analyzer.py && python3 skin/skin_optimizer.py

      # Step 6: Commit and push changes
      - name: Commit and push changes
        run: |
          git config user.email "github-action@example.com"
          git config user.name "GitHub Action"
          git add skin/skin_analytics.json skin/skin_optimization_index.json
          git commit -m "自動更新: skin analytics $(date '+%Y/%m/%d %H:%M' -u)" || true
          git pull --rebase origin main
          git push
